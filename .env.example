# Example .env for Sistema di Gestione Eventi
# COPY THIS TO .env and adjust values for your environment. Never commit the real .env to version control.

# APP
APP_ENV=production
# Set to 1 for development behaviour (display errors, etc.)
APP_DEBUG=0

# Database
DB_HOST=127.0.0.1
DB_NAME=your_database_name
DB_USER=your_db_user
DB_PASS=your_super_secret_password

# Error log path (optional). Defaults to ./logs/error.log
# If specifying an absolute path, ensure the webserver user can write to it and it's outside the web-root
ERROR_LOG=./logs/error.log

# Session cookie settings
SESSION_COOKIE_SAMESITE=Lax
SESSION_COOKIE_SECURE=0
